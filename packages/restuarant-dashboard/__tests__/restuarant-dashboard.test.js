'use strict';

const restuarantDashboard = require('..');
const assert = require('assert').strict;

assert.strictEqual(restuarantDashboard(), 'Hello from restuarantDashboard');
console.info('restuarantDashboard tests passed');
